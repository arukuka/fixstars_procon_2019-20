name: C/C++ CI

on:
  push:
  pull_request:

jobs:
  build:

    runs-on: ubuntu-18.04

    steps:
    - uses: actions/checkout@v2
    - run: g++ -std=gnu++14 -O2 -Wall -o a.out arukuka.cpp
    - run: clang++-9 -std=c++14 -O2 -Wall -o a.out arukuka.cpp
